#!/bin/bash

echo "Refreshing python tools... "
python -m pip install -U --upgrade wheel setuptools pybind11 pip nox invoke

echo "Installing keyring packages... "
python -m pip install -U keyring artifacts-keyring

echo "All done."
